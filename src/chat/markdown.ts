/** @deprecated Use `./markdown/index` — this file exists for import path compatibility. */
export {
  mountMarkdown,
  renderAiText,
  renderMarkdown,
  sanitizeHtml,
  type MarkdownMount,
  type MountMarkdownOptions,
} from "./markdown/index";
