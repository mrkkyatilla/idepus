[package]
name = "idepus-plugin-api"
version = "0.1.0"
edition = "2021"
description = "dylib plugin API for idepus extensions"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"

[lib]
crate-type = ["rlib"]
